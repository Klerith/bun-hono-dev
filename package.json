{
  "name": "my-server",
  "module": "index.ts",
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "5.3.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun prisma migrate deploy",
    "start": "bun prisma migrate deploy && bun src/index.ts"
  },
  "type": "module",
  "dependencies": {
    "@hono/zod-validator": "^0.1.9",
    "@prisma/client": "5.3.1",
    "env-var": "^7.4.1",
    "hono": "^3.7.2"
  }
}